Fix two bugs and implement UI improvements in knx-web-app/frontend/src/Settings.jsx and knx-web-app/frontend/src/index.css (or wherever the CSS lives).

BUG FIX 1: 'Scene not found' when linking Hue scene to a new room.
Root cause: openHueSceneModal tries to link a scene by ID, but if the user added scenes to a new room without saving first, those scenes don't exist in the backend yet.
Fix: At the start of openHueSceneModal, before fetching Hue scenes, auto-save current rooms state by calling: await updateConfig({ rooms }); — This ensures scene IDs exist in backend before linking.

BUG FIX 2: 'Generate Base Scenes' adds shade scenes (Issue #16).
In handleGenerateBaseScenes, currently adds 4 scenes: Off (1), Bright (2), Shades Up (3), Shades Down (4).
Fix: Only generate Off (1) and Bright (2). Remove Shades Up and Shades Down.

UI FIX 3: Hue pill styling.
- .hue-linked-badge CSS: background #7B5EA7, color white, border-radius 999px, padding 4px 10px, display inline-flex, align-items center, gap 6px, font-size 0.82rem. Remove ugly borders.
- .hue-unlink-btn CSS: background transparent, border none, color white, cursor pointer, font-size 1rem, line-height 1, padding 0, margin-left 2px, opacity 0.8, hover opacity 1.
- In SortableSceneRow: if scene name matches /^(aus|off)$/i AND hueStatus.paired AND isLight — do NOT show Link Hue button. Instead show: <span className="hue-off-label">This scene will turn off the hue room.</span>
- .hue-off-label CSS: font-size 0.75rem, color #888, font-style italic.
- .scene-field--hue CSS: add margin-left auto, width auto, min-width 0 so it right-aligns.

After all changes, commit with message: fix: auto-save before Hue scene link, remove shade base scenes, improve Hue pill UI
Then push to origin feature/issue-14-hue-scene-linking.
Then run: openclaw system event --text "Done: PR15 bugs fixed and UI improved" --mode now
